// Stub for next/dynamic
const dynamic = (loader: any, _opts?: any) => {
  const DynamicComponent = (props: any) => null;
  DynamicComponent.displayName = 'DynamicComponent';
  return DynamicComponent;
};
export default dynamic;
