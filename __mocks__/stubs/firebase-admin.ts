// Stub for firebase-admin — used only during vitest when full deps are not installed
export const initializeApp = () => ({});
export const getApps = () => [];
export const cert = () => ({});
export const getApp = () => ({});
export default { initializeApp, getApps, cert, getApp };
