/**
 * Mobile Autodraft Limits Demo Page
 * 
 * Demonstrates the AutodraftLimitsMobile component
 */

import React, { useState } from 'react';
import AutodraftLimitsMobile from '../../components/mobile/AutodraftLimitsMobile';

export default function AutodraftLimitsDemoPage() {
  const [showModal, setShowModal] = useState(false);

  return (
    <div className="min-h-screen bg-gray-900 flex items-center justify-center p-4">
      <div className="text-center space-y-6">
        <h1 className="text-3xl font-bold text-white mb-2">
          Autodraft Limits Demo
        </h1>
        <p className="text-gray-400 mb-8">
          Configure position-specific autodraft limits for best ball drafts
        </p>
        
        <button
          onClick={() => setShowModal(true)}
          className="px-8 py-4 bg-blue-500 hover:bg-blue-600 text-white font-bold rounded-lg transition-colors"
        >
          Open Autodraft Limits
        </button>

        <div className="mt-8 text-left bg-gray-800 rounded-lg p-6 max-w-md">
          <h3 className="text-white font-bold mb-4">Features:</h3>
          <ul className="text-gray-300 space-y-2 text-sm">
            <li>• Position-specific color coding</li>
            <li>• Interactive sliders and number buttons</li>
            <li>• Default limits: QB(3), RB(6), WR(7), TE(3)</li>
            <li>• Local storage persistence</li>
            <li>• Mobile-optimized with hidden scrollbars</li>
            <li>• Reset to defaults functionality</li>
          </ul>
        </div>
      </div>

      <AutodraftLimitsMobile 
        open={showModal} 
        onClose={() => setShowModal(false)} 
      />
    </div>
  );
}
