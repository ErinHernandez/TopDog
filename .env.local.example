# ─────────────────────────────────────────────────────────────────────────────
# TopDog Studio — Environment Variables Template
# ─────────────────────────────────────────────────────────────────────────────
#
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# IMPORTANT: Never commit .env.local to git.
# Production secrets belong in the Vercel Environment Variables dashboard.
# ─────────────────────────────────────────────────────────────────────────────

# ── Firebase (client-side, safe to expose) ──
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_USE_NEW_DRAFT_ROOM=true

# ── Studio Auth (NextAuth) ──
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# ── GitHub OAuth App ──
# Create at: https://github.com/settings/developers
# Callback URL: <NEXTAUTH_URL>/api/auth/callback/github
GITHUB_ID=
GITHUB_SECRET=

# ── Studio Access Control ──
# Comma-separated GitHub usernames (case-insensitive).
# Leave empty or remove to allow ALL GitHub users (open access).
STUDIO_ALLOWED_GITHUB_USERS=

# ── Anthropic API ──
ANTHROPIC_API_KEY=

# ── PayPal (optional) ──
NEXT_PUBLIC_PAYPAL_ENABLED=false
PAYPAL_SANDBOX_CLIENT_ID=
PAYPAL_SANDBOX_CLIENT_SECRET=
PAYPAL_SANDBOX_WEBHOOK_ID=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_WEBHOOK_ID=

# ── Studio Feature Flags ──
# STUDIO_PII_DETECTION=true
# STUDIO_PII_BLOCK_CRITICAL=true
# STUDIO_PII_CUSTOM_PATTERNS=[]
# STUDIO_RATE_LIMIT_BACKEND=memory
